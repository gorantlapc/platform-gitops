---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service-deployment
  namespace: development
  labels:
    app: notification-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: 593793049094.dkr.ecr.eu-north-1.amazonaws.com/dev-notification-service:37df72b2d7e60f7090b7f288b77a0c60285605fb
#          imagePullPolicy: Never - This is commented out to allow Kubernetes to pull the image from ECR. If you want to use a local image, uncomment this line and ensure the image is built locally.
          ports:
            - containerPort: 8087
          env:
            - name: LOCALSTACK_ENDPOINT
              value: http://localstack:4566
            - name: SPRING_PROFILES_ACTIVE
              value: dev
            - name: AWS_REGION
              value: us-east-1
            - name: AWS_SQS_QUEUE_NAME
              value: notification-queue
            - name: SES_FROM_ADDRESS
              value: gorantla.gpc@gmail.com